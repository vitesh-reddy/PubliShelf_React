<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Signup - PubliShelf</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link rel="stylesheet" href="/css/styles.css">

    <style>
        :root {
            --purple-50: #f5f3ff;
            --purple-500: #8b5cf6;
            --purple-600: #7c3aed;
            --purple-700: #6d28d9;
            --indigo-600: #4f46e5;
            --gray-50: #f9fafb;
            --gray-300: #d1d5db;
            --gray-400: #9ca3af;
            --gray-600: #4b5563;
            --gray-700: #374151;
            --gray-900: #111827;
            --red-500: #ef4444;
            --white: #ffffff;
            --black: #000000;
        }

        body {
            background: var(--gray-50);
            font-family: system-ui, -apple-system, sans-serif;
            margin: 0;
            min-height: 100vh;
        }

        /* Container and wrapper */
        .container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 3rem 1rem;
            background: linear-gradient(to bottom, var(--purple-50), var(--white));
        }

        .signup-wrapper {
            max-width: 28rem;
            width: 100%;
        }

        /* Logo section */
        .logo-section {
            text-align: center;
            margin-bottom: 2.5rem;
        }

        .logo {
            display: inline-block;
            text-decoration: none;
        }

        .logo-text {
            font-size: 2.25rem;
            font-weight: bold;
            background: linear-gradient(to right, var(--purple-600), var(--indigo-600));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .title {
            margin-top: 1.5rem;
            font-size: 1.875rem;
            font-weight: 800;
            color: var(--gray-900);
        }

        .subtitle {
            margin-top: 0.5rem;
            font-size: 0.875rem;
            color: var(--gray-600);
        }

        /* Form container */
        .form-container {
            background: var(--white);
            padding: 2rem;
            border-radius: 0.75rem;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            animation: fadeIn 0.5s ease-out;
        }

        /* Form elements */
        .name-fields {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1.5rem;
            margin-bottom: 0.5rem;
        }

        .form-group {
            margin-bottom: 1.25rem;
        }

        label {
            display: block;
            font-size: 0.875rem;
            font-weight: 500;
            color: var(--gray-700);
            margin-bottom: 0.5rem;
        }

        .input-icon-wrapper {
            position: relative;
        }

        .input-icon-wrapper i {
            position: absolute;
            left: 0.75rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--gray-400);
            pointer-events: none;
        }

        input[type="text"],
        input[type="email"],
        input[type="password"] {
            width: 100%;
            padding: 0.5rem 0.75rem;
            padding-left: 2.5rem;
            border: 1px solid var(--gray-300);
            border-radius: 0.5rem;
            font-size: 0.875rem;
            transition: all 0.2s;
            box-sizing: border-box;
        }

        input:focus {
            outline: none;
            border-color: var(--purple-500);
            box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.2);
        }

        .toggle-password {
            position: absolute;
            right: 0.75rem;
            top: 55%;
            transform: translateY(-50%);
            background: none;
            border: none;
            cursor: pointer;
            color: var(--gray-400);
            padding: 0;
        }

        .toggle-password:hover {
            color: var(--gray-600);
        }

        /* Terms checkbox */
        .terms-group {
            display: flex;
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .terms-group input[type="checkbox"] {
            width: 1rem;
            height: 1rem;
            margin-right: 0.5rem;
            cursor: pointer;
        }

        .terms-group label {
            margin: 0;
            font-size: 0.875rem;
        }

        /* Links */
        .link {
            color: var(--purple-600);
            text-decoration: none;
            transition: color 0.2s;
        }

        .link:hover {
            color: var(--purple-500);
        }

        /* Submit button */
        .submit-button {
            width: 100%;
            padding: 0.75rem 1rem;
            background: var(--purple-600);
            color: var(--white);
            border: none;
            border-radius: 0.5rem;
            font-size: 0.875rem;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s;
        }

        .submit-button:hover {
            background: var(--purple-700);
            transform: translateY(-2px);
        }

        .submit-button:focus {
            outline: none;
            box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.5);
        }

        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Responsive adjustments */
        @media (max-width: 640px) {
            .name-fields {
                grid-template-columns: 1fr;
            }

            .form-container {
                padding: 1.5rem;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="signup-wrapper">
            <!-- Logo -->
            <div class="logo-section">
                <a href="/" class="logo">
                    <span class="logo-text">PubliShelf</span>
                </a>
                <h2 class="title">Create Admin Account</h2>
                <p class="subtitle">
                    Already have an account?
                    <a href="/auth/login" class="link">Sign in</a>
                </p>
            </div>

            <!-- Signup Form -->
            <form action="/admin/signup" method="POST" class="signup-form">
                <div class="form-container">
                    <div class="name-fields">
                        <div class="form-group">
                            <label for="firstName">First Name</label>
                            <input type="text" id="firstName" name="firstName" required>
                        </div>
                        <div class="form-group">
                            <label for="lastName">Last Name</label>
                            <input type="text" id="lastName" name="lastName" required>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="employeeId">Employee ID</label>
                        <div class="input-icon-wrapper">
                            <i class="fas fa-id-badge"></i>
                            <input id="employeeId" name="employeeId" type="text" required placeholder="EMP-12345">
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="email">Work Email</label>
                        <div class="input-icon-wrapper">
                            <i class="fas fa-envelope"></i>
                            <input id="email" name="email" type="email" required placeholder="admin@publishelf.com"
                                required>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="password">Password</label>
                        <div class="input-icon-wrapper">
                            <i class="fas fa-lock"></i>
                            <input id="password" name="password" type="password" required placeholder="••••••••"
                                required>
                            <button type="button" onclick="TogglePasswordFun()" class="toggle-password">
                                <img src="https://img.icons8.com/?size=100&id=85130&format=png&color=000000"
                                    style="height: 25px; width: 25px;" alt="">
                            </button>
                            </button>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="securityKey">Admin Security Key</label>
                        <div class="input-icon-wrapper">
                            <i class="fas fa-key"></i>
                            <input id="securityKey" name="securityKey" type="password" required
                                placeholder="Enter security key" required>
                        </div>
                    </div>

                    <div class="terms-group">
                        <input id="terms" name="terms" type="checkbox" required>
                        <label for="terms">
                            I agree to the
                            <a href="#" class="link">Terms of Service</a>,
                            <a href="#" class="link">Privacy Policy</a>,
                            and
                            <a href="#" class="link">Security Policy</a>
                        </label>
                    </div>

                    <button type="submit" class="submit-button">
                        Create Admin Account
                    </button>
                </div>
            </form>
        </div>
    </div>
    <script>
        function TogglePasswordFun() {
            const pass = document.getElementById('password');
            if (pass.type == 'password')
                pass.type = 'text';
            else
                pass.type = 'password';
        }
    </script>

    <script src="/js/admin-signup.js"></script>
</body>

</html>